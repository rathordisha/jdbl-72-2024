package com.example.L12_minor_project_1.enums;

import jakarta.persistence.Enumerated;

// when we know the value of the variable names we define them as constants.
public enum Role {
    ADMIN,
    GATEKEEPER,
    RESIDENT;
}
